# AGENTS.md · Madehall 规格构建阶段（对应 SOP-1）

> **适用范围**：SOP-1 全程（传统三文档咬合 → AI 种子文档 → 六件套定稿），不含写代码阶段。
> **上位文件**：`docs/0.0方案文档/1.Agent-first·1·开发前规格构建方案.md`（指导手册）＋ `docs/0.0方案文档/各阶段SOP/SOP-1-传统三文档咬合与六件套定稿.md`（本阶段 SOP）。本文与上位文件冲突时以上位文件为准，并向负责人报告冲突。
> 本文件为根目录本体、唯一活文件；阶段结束时按 SOP-1 §4.4 存归档快照，平时不存副本。

## 规则（上限 20 条，现 18 条）

1. **角色边界**：Agent 生成与修改一切源文件、配置工具链、跑校验；负责人只在渲染视图上审核、批注、裁决。派生视图（文档站、快照、报告）任何人不得手改。
2. **事实源顺位**：负责人当场裁决 ＞ PRD（`Madehall_PRD.md`）＞ 业务路径手册 ＞ 统一原型（仅交互与视觉参照，其数值与文案不构成需求依据）。《00-需求差异矩阵》不入库、不引用。
3. **冲突处理**：PRD 与手册的任何不一致（数值、口径、单侧缺失、双侧未定）一律登记进《咬合差异登记表》交负责人裁决，不静默采信任何一侧；Agent 只提供裁决建议＋两侧原文并排证据。
4. **业务裁决边界**：凡改变业务事实（数值、口径、状态、权限）的修订，登记表"负责人裁决"列非空才可执行；Agent 可自行执行的仅限不改变业务事实的动作（版本号统一、告示核销、映射回写、格式修订）。
5. **落盘路径**：产出只落 SOP-1 §一产出表所列路径；不得新建路径、不得改名。
6. **单一事实来源**：业务规则只在③、数据类型只在②、接口只在④、UI 与交互只在①、架构决策只在 `adr/`；其余位置一律引用，禁止复制定义。
7. **单向瀑布**：下游发现上游缺漏，登记《漏洞回流登记表》、回上游修、重新生成下游；禁止就地补。涉需求裁决的攒批交负责人，不逐条打断。
8. **REQ-ID 追溯**：PRD vFinal 固化 `REQ-<章节号>-<序号>` 后，一切引用只用 REQ-ID 不用章节号；标注机制按件——①属性 `data-req-id`、②JSDoc `@req`、③YAML `req:`、④OpenAPI `x-req:`、⑥用例注释 `@req`；全部登记进 `PRD覆盖矩阵.yaml`。
9. **规则命名**：`RULE-<域>-<序号>`，域＝准入/T1/T2/T3/FFE/PRICE/PERM/AUDIT；id 全局唯一，是①与⑥引用规则的唯一句柄。
10. **未落值参数**：一律 `🟡n`（或 `SPIKE-n`）占位并挂《未落值与待验证清单》；标注按件——②JSDoc `@pending 🟡n`、③`status: pending_param`＋`param_ref`、④`x-status: pending-spike`、⑥`test.skip`＋`pending: 🟡n`。禁止写死猜测值让规格或用例"看起来完整"。
11. **覆盖口径**：覆盖率一律三数呈现「已覆盖 X / 未验收 Y / 总 N」，分母恒为全集；禁止 `X/(N−Y)` 式报告；Y > 0 时报告标题写「全绿 · 含 Y 项未验收」，pending 清单随报告呈负责人。
12. **废止反查**：任何交付物生成后，对照种子附录"废止能力清单"做 grep 级自查（AI 类字样、EUR、批发等级、Net terms、Tier2 即时报价、拆单、项目登记独家锁定等）；命中即返工，不入库。
13. **语言与措辞**：对客文案全英文（EN 版随中文定稿统一出）、内部后台中英双语；对客品牌一律 Madehall，正文零 Fabbrio（文件名与历史引文除外）；禁用措辞以手册 0.5 第 3 条清单为准，注意其中 AI 项的划界（"自动报价/参考价计算器"不算命中）。
14. **①的注释与语义**：HTML 只允许 `SEMANTIC-CHECK` 与 `API` 两类注释，禁止在注释中定义业务逻辑；使用语义标签，禁止 div/span 模拟交互控件；关键元素预埋 `data-testid`；受业务规则约束的元素标 `data-rule-id="RULE-xxx"` 指向③。
15. **②的纪律**：每个字段必有 JSDoc（业务含义、约束、示例）；禁止 `any`/`unknown`；金额一律分、时间一律毫秒；枚举用字面量联合类型；JSON Schema 与 Zod/Ajv schema 一律由②生成，禁止手写。
16. **④的纪律**：输入源限三处——①的交互、②的类型、种子§4 非 UI 能力清单，三处之外的接口不得出现；每个接口必有语义化 `operationId`；错误响应结构化（`code`＋`message`），`code` 与③的规则 id 可映射。
17. **变更影响申报**：修改基础层（①②③）任一文件前，先列出受影响的下游清单（④⑤⑥中哪些需同步更新），确认后一并修改；禁止只改上游不动下游。
18. **验证与报告**：说"完成/通过"必须附命令＋真实输出；文档、结构、只读检查不得表述为"测试通过"；工具或卡点不可用时写明不可用项、替代检查与影响范围，降级结果不得表述为通过。

## 附录 · 运行时事实（不计入 20 条）

### 阶段与闸门（当前所处：P1）

P1 生成本文件（G1＝负责人审核通过）→ P2 咬合核查（G2＝登记表清零，PRD/手册出 vFinal＋REQ-ID 固化）→ P3 种子文档（G3＝`--phase=p3` 退出码 0＋负责人审核）→ P4 六件套＋A 组卡点（G4＝A 组全绿＋负责人审核）→ P5 规格定稿冻结。闸门未过不开工下一阶段。

### 输入文件（均在 `docs/0.1种子文件(传统+AI)/传统种子文件/`）

| 编号 | 文件 | 修改权限 |
| :- | :--- | :--- |
| I1 | `Madehall_PRD.md` | **P2 内按负责人裁决原路径修订出 vFinal**（P3 起因回流修订时同样凭裁决）；其余情形不改 |
| I2 | `1-业务路径核查手册-v1.3.md` | 同 I1 |
| I3 | `Madehall_unified_prototype_ZH.html` | 全程只读 |
| I4 | `2.2-负责人走查问题登记表-v2.0.md` | 全程只读 |

四件在 P5 一并标注冻结声明；冻结后全部转为只读历史输入。

### 产出路径（完整表见 SOP-1 §一）

| 产出 | 路径 |
| :- | :--- |
| 咬合差异登记表 | `docs/0.1种子文件(传统+AI)/咬合差异登记表.md` |
| 漏洞回流登记表 | `docs/0.1种子文件(传统+AI)/漏洞回流登记表.md` |
| AI 需求种子文档 | `docs/0.1种子文件(传统+AI)/AI种子文件/Madehall-AI需求种子文档.md` |
| PRD 覆盖矩阵 | `docs/0.1种子文件(传统+AI)/PRD覆盖矩阵.yaml` |
| 未落值与待验证清单 | `docs/0.1种子文件(传统+AI)/未落值与待验证清单.md` |
| 覆盖校验脚本 | `scripts/check-req-coverage.mjs` |
| 六件套①–⑥ | `docs/1.六要素/` 按件分目录 |

### 命令清单

| 用途 | 命令 | 状态 |
| :- | :--- | :--- |
| 覆盖矩阵校验 | `node scripts/check-req-coverage.mjs --phase=<阶段>`，`<阶段>` 按当前所处阶段取 `p3`、`p4`、`p5` 之一（如 P3 期间执行 `node scripts/check-req-coverage.mjs --phase=p3`）；判据＝退出码 0；每次打印 X/Y/N 三数；台账缺失即失败，不得按 Y=0 处理 | 脚本待 P3 前写好 |
| A 组卡点 | A1 构建型视图随源重建 / A2 ③schema 校验＋规则冲突检测 / A3 规则引用完整性（①↔③↔⑥）/ A4 ④与②同源比对 | 待 P4 搭建后回填具体命令 |
| 视图渲染 | ①浏览器直开（用到 ES module/fetch 时起本地 preview server）；②typedoc；③决策表渲染脚本；④scalar；⑤Obsidian vault 指向仓库根 | 待 P4 搭建后回填具体命令 |

命令落地后回填本表并删除"待回填"字样；回填前不得在任何报告中声称对应检查已执行。
