
> **定位**：这是 [[0.2.AI 时代 L4 基础设施构建者：全链路认知与执行协议|AI 时代 L4 基础设施构建者：全链路认知与执行协议]] 的第一个实践项目手册。协议回答"应该怎样工作"，本手册回答"在 Fabbrio 这个真实项目里，今天具体怎么做"。
> 给"项目负责人兼开发负责人"的日常手册：不要求成为日常编码者，但要能用 Harness、证据与关键代码抽查，稳定交付一个真实商业系统。
> 基线快照：2026-07-21。文中标 ※ 的数字随项目演进变化，一切以主控台派生快照为准。

## 0. 四文档体系与你的角色

本手册是四文档体系中的**项目侧操作层**。体系的逻辑：世界观回答"价值为什么迁移"，方法论回答"应该怎样工作"，落到操作层裂变为两条腿——系统侧控制项目，人侧维持系统所依赖的判断力下限；两条腿由逃逸缺陷**双登记**互相校准（闸门归因进 R 账本补闸门，能力归因进学习日志回炉单元，见 §5.3 / §8.1 第 5 条）。两本账都已存在、各有唯一位置，**不另建副本**：R 账本＝仓库主控台 §6 风险索引（`文档/02-项目维护/00-项目状态与交付索引.md`，受 docs_lint 机器闸门管辖）；学习日志＝[[学习日志]]（`2.Agent_harness_engineering/`，人侧私账不进仓库）：

| 层         | 文档                                | 回答的问题                           |                                                                        |
| --------- | --------------------------------- | ------------------------------- | ---------------------------------------------------------------------- |
| 世界观       | [[0.1.AI时代原生人类：认知、学习与价值创造方式的重构    | 《AI时代原生人类》]]                    | 人的价值为什么从"记住更多、执行更快"迁移到"定义问题、设计系统、组织智能、判断价值"                            |
| 方法论       | [[0.2.AI 时代 L4 基础设施构建者：全链路认知与执行协议 | 《L4 全链路认知与执行协议》]]               | 编译链、Loop Engineering、资产化阈值、人机分界、熔断                                     |
| 操作层 · 项目侧 | 本手册                               | 在 Fabbrio 的具体文件、命令、闸门和节律上，今天怎么做 |                                                                        |
| 操作层 · 人侧  | [[3.Ai_native_coding_engineering  | Ai_native_coding_engineering]]  | 评审者底线能力（读码、调试、架构判断）怎么建立、验收与维持——本手册 §5.3 审判能力下限的来源，L4 协议 §3.1 失效条件的补课程序 |

你的角色不是代码生产者，而是**交付控制者**。你负责让每一项改变满足四条：

- 业务目标和范围没有被 Agent 擅自改写；
- 钱、订单、权限、文件和 AI 的边界没有被破坏；
- 实现、测试、人工验收与上线事实能够相互证明；
- 未验证的真实集成不被包装成"已完成"。

编程能力弱不是把控项目的障碍，前提是承认它的真实含义：你不逐行读完整仓库，但对高风险链路，必须能沿"**页面 → API → 服务 → Shopify / FF-DB / 存储 → 审计与测试**"追踪一遍，并提出正确的问题。低并发、低访问量只降低性能设计的复杂度，**不降低金额、权限、数据隔离、第三方同步和上线变更的风险**。

《AI时代原生人类》给出的四项核心能力——**高质量认知、系统化拆解、AI 协同、价值判断**——不是抽象口号，它们分别对应本手册的第 4 章（拆解为变更契约）、第 3 章（组织多会话协作）、第 5 章（判断代码真伪）、第 7 章（判断证据与放行）。这个项目就是这四项能力的日常训练场。

## 1. 项目一页纸：Fabbrio 是什么

**产品**：B2B 家具报价系统 MVP。Shopify Advanced 承载交易层；自研 Fabbrio 引擎 / FF-DB 承载配置器、RFQ、AI 辅助拆单、报价计算、CAD 版本、状态机与商户审核。

**三层业务（严禁混用各自的订单与状态逻辑）**：

- **Tier1 现货**：Shopify 标准 checkout；
- **Tier2 半定制**：配置器 → 后端报价 → Shopify Draft Order 锁价；
- **Tier3 全定制**：RFQ → 真人报价 / CAD 资产 → 30% 定金链路；
- 另有 **FF&E 整单工作台**与 **AI 拆单**（仅 beta 辅助，不自动决策）。

**技术栈**：

| 层 | 选型 |
|---|---|
| 框架 | Next.js 16（App Router + Route Handlers）、React 19、TypeScript |
| 数据库 | Neon Postgres + Drizzle ORM（运行时 pooled `DATABASE_URL`，迁移 direct `DIRECT_URL`） |
| 运行时 | Node 24、pnpm 11.7.0（monorepo，唯一应用包 `@fabbrio/web`） |
| 测试 | Vitest（单测/契约/DB 测试）+ Playwright（E2E） |
| 集成 | Shopify Admin、S3 对象存储（短签 URL）、SES 邮件、OpenAI 兼容 LLM（拆单 beta） |

**仓库地图**（`D:\file\factory_os_dev`）：

- `apps/fabbrio/` — 唯一生产应用：`app/`（页面与 Route Handlers）、`server/`（服务层与集成边界，高风险代码全在这里）、`e2e/`（Playwright）；
- `文档/01-开发文档/` — 需求、技术方案、ADR（15 个※）、`4.功能开发与测试/00-功能清单.md`（F001–F502 索引）；
- `文档/02-项目维护/` — **上下文恢复主入口**（见下）；
- `scripts/` — 治理脚本：`docs_lint.py`、`assemble_status.py`、`next_id.py`、`secret_scan.py`、`IDS.json`；
- 根目录 — `AGENTS.md`（仓库级 agent 规则与产品契约，最高优先级）+ 各 SOP（`单功能开发SOP.md`、`单功能交叉复核SOP.md`、`文档治理SOP.md`、`多会话协作SOP.md`）。

**上下文恢复入口**——每个新会话（人或 Agent）从这三个文件开始，不靠任何人的记忆：

1. `文档/02-项目维护/00-项目状态与交付索引.md`（**主控台**）：§0 派生快照、§4.1 任务账本、§5 决策索引（DEC-xxx）、§6 风险索引（R-xxx）；
2. `文档/02-项目维护/01-问题验证发布记录.md`：VR-xxx 验证记录登记处，**append-only 只增不改**；
3. `AGENTS.md`：产品契约、事实源顺序、状态机、完成标准。

**当前基线（※2026-07-21）**：任务 83 项、已完成 72；单测文件 222 个；E2E 13 spec / 96 例；编号高水位 VR-184 / DEC-064；未完成主线：PM-605、PM-609/610/611（真实集成批）、PM-612（进行中）、PM-701–706。

## 2. 不可突破边界（产品契约）

这五条是 Fabbrio 的宪法级边界，来源于 `AGENTS.md` 与 ADR：

| 领域 | 真相源与规则 |
|---|---|
| Shopify | 账户、Company、Catalog、Price List、Product/Variant、Draft Order、Order、收款、退款与履约的**交易事实源** |
| FF-DB | 配置规则、PricingRule、RFQ、Quote、BOM、CAD、Tier2/3 订单镜像、OrderState、Merchant、AuditLog 的**事实源** |
| 报价 | **后端计算为真值**；前端只能展示；Tier2/3 经 Shopify Draft Order 承接交易 |
| 权限与文件 | 前端无 Admin/LLM/存储写密钥；CAD 私有存储，按 `company_id` 鉴权、后端短期签名 URL |
| AI | 仅 beta 辅助；不自动批准、不自动派单、不生成制造级 CAD；**人工逐行确认是放行条件** |

**检验标准**：任何 PR 只要触碰这五条之一，自动升为高风险审查（第 5 章），没有例外。Agent 提出"简化"或"顺手改一下"这五条相关逻辑时，一律先登记、后裁决，不允许"按常识选一个方案"。

## 3. 人机分界与多会话工作法

### 3.1 责任分界表

| 事项 | 负责人必须决定或亲自执行 | Agent 可执行 |
|---|---|---|
| 目标与范围 | 事实源、优先级、取舍、上线边界 | 整理冲突、提出方案 |
| 业务规则 | 报价、状态机、权限、例外与人工兜底 | 按已裁决规则实现/测试 |
| 代码实现 | 审查高风险关键路径与变更证据 | 编码、测试、PR、文档草稿 |
| 不可逆操作 | 合并 `main`、真实凭据、支付/生产配置、最终放行 | 准备步骤、证据、回滚方案 |
| 完成判断 | 认可或拒绝风险、确认真实验证范围 | 汇总 CI、复核、剩余风险 |

原则（同 L4 协议 §3.1）：Agent 提供候选实现与证据；负责人拥有业务裁决权和风险接受权。

### 3.2 多会话工作法

实践中每个 PM 任务开三个会话，独立性来自**不共享对话上下文**：

- **项目管理会话**：把任务编译成任务书与提示词（变更契约，见附录模板）；
- **执行会话**：在允许范围内实现、写测试、准备 PR 与技术说明；
- **审核会话**：不复用执行对话，独立审查 diff、测试检出力、越界与风险。

验收或纯文档任务可减为"执行 + 独立核查"两会话（依据 `多会话协作SOP.md`）。不要为了"多 Agent"而串行堆角色：**独立性、明确输入与可验证证据，比角色数量更重要。**

还要认清这种独立性的边界：会话独立只是**上下文独立，不是模型独立**——执行会话与审核会话可能是同族模型，盲区相关，两个 Agent 都觉得"合理"的错误，多开会话拦不住。复核冗余不能替代确定性闸门与人工抽查（判据见 §5.3）。

**本项目的一条真实教训**：曾发生执行会话汇报与实际情况不符的案例。由此确立强制纪律——**Agent 的自述永远不是证据**；只有命令的实际输出、CI 运行记录、diff、截图和账本登记才是。任何"已完成"的声明，都要能指出对应的证据位置。

## 4. 七步交付闭环

这是 L4 协议核心协议链在 Fabbrio 的落地形态。"代码完成"只是第 3 步；只有第 7 步结束，功能才可能称为完成。

1. **取证定界**：读主控台、`AGENTS.md`、相关规格卡（`文档/01-开发文档/3.技术方案/03-开发任务拆分.md`）和现有代码，确认真实状态，不信任何转述。
2. **设计任务**：产出业务信息流图、状态/数据/API 契约和变更契约（附录模板）。有矛盾先登记再裁决。
3. **受限实现**：执行会话只在允许范围内修改；新行为覆盖正常、边界和失败路径（本项目以 TDD 先红后绿为常规做法）。
4. **机器验证**：实际运行 `pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build` / `pnpm secret:scan` / `python scripts/docs_lint.py`，涉及 UI 加 `pnpm --filter @fabbrio/web test:e2e`，涉及 schema/持久化触发 DB Migration Test（第 6 章）。
5. **独立复核**：审核会话审查 diff、测试和风险，出具复核记录；有条件通过的条件必须逐项闭环。
6. **人工闸门**：负责人审阅证据；真实支付、真实凭据、生产配置等由负责人亲自操作，Agent 只准备步骤与回滚方案。
7. **main 回读与回写**：负责人亲手合并（当前靠 DEC-001 流程约定兜底；GitHub Pro 已就绪、平台级分支保护属遗留行动项待配置）；合并后回读 `main` 侧 Basic Gates；回写 `00-功能清单.md`、主控台 §4.1/§5/§6、VR 登记。编号用 `python scripts/next_id.py` 从 `scripts/IDS.json` 高水位分配，杜绝撞号；编辑主控台 §4.1 后必须补跑 `python scripts/assemble_status.py` 同步 §0 派生区，`docs_lint` 会兜底拦截不一致。

**检验标准**：随机抽一个已完成任务，能沿"任务书 → PR → CI run → VR 编号 → 主控台账本"走通完整证据链。走不通的任务不算完成，登记回§6 风险。

## 5. 你应该读多少代码

### 5.1 风险分级审查

| 变更级别 | 示例 | 你的最低审查方式 |
|---|---|---|
| 低风险 | 文案、样式、只读页面布局 | 看变更摘要、截图、E2E 结果与少量 diff |
| 中风险 | 普通 CRUD、列表、筛选 | 看信息流图、接口契约、公司权限判断、测试；抽查 API 与服务层 diff |
| 高风险 | 报价、Draft Order、付款/退款、Webhook、登录授权、跨公司数据、CAD/S3、AI 批准、数据库迁移 | 读关键路径代码与测试；要求独立复核和人工闸门 |

高风险时不必理解每一行 TypeScript。你只需能回答七个问题：

1. 用户从哪个页面或外部事件进入？
2. 哪个 API 接收请求，并怎样认证、按公司隔离？
3. 哪个服务执行业务规则与状态转换？
4. 金额、订单、权限或文件的真值归属在哪里？
5. 最终写入 Shopify、FF-DB 或对象存储的是什么？
6. 重复、越权、金额不一致、第三方失败时，系统会怎样拒绝、记录和恢复？
7. 哪个测试实际证明了上述结论？

**如果 Agent 无法把这七个问题讲清楚，它的"完成"结论不能采信。**

### 5.2 高风险模块地图

高风险代码集中在 `apps/fabbrio/server/`，抽查时按这张表定位：

| 模块 | 功能（功能单元号） | 你抽查什么 |
|---|---|---|
| `server/quote-engine/` | 报价引擎纯计算（F002）：逐行卷积、金额用整数最小货币单位 | 金额不变量测试；前端是否只展示不算价 |
| `server/state-machine/` | RFQ/Order 状态机唯一权威（F001） | 非法状态转换是否被白名单拒绝并审计 |
| `server/shopify/` | Quote → Draft Order 锁价、补偿 outbox、对账（F006/F202） | 金额/币种/行项对应关系；失败补偿与幂等 |
| `server/auth/` | 会话↔`company_id` 绑定、跨公司 404 掩蔽、六角色权限点（F004） | 越权负例测试；前端是否零凭据 |
| `server/db/` | Drizzle schema、RLS 租户隔离、迁移 | `rls-tenant-isolation.db.test.ts`；迁移是否可回滚 |
| `server/rfq/` | RFQ 流转、30% 定金（half-up）、Tier3 报价（F301/302/305） | 定金取整规则；状态流转经状态机而非旁路 |
| `server/cad/` | CAD 版本只增不改、短签下载（F303） | 按 `company_id` 鉴权；签名 URL 是否短期 |
| `server/ai/` | LLM 拆单 beta、人工闸门守卫（F304） | confidence 高低都不自动放行；mock 与真实调用边界 |
| `server/webhooks/` | Shopify webhook：HMAC、幂等、乱序守卫、定时对账（F501） | 签名校验；重复投递去重；对账兜底 |

**检验标准**（同 L4 协议 §3.2）：读码范围与风险成正比，不与代码规模成正比；同一类问题反复需要人工审查，即为 Harness 缺口，应固化为测试、Lint、Hook 或规则。

### 5.3 承重结构：必须亲自读的最小集

前两节回答"读多少"，本节回答"读什么"。先摆正前提：在这套体系里，代码不是被少读了，而是被**委托读**了——独立复核会话、类型系统和测试都在读。你的任务是审计委托方，而审计对象是承重结构，不是填充物。

**必须亲自读的（承重结构，占全库比例小，承载绝大部分灾难性风险）**：

- **守卫片段**：鉴权检查、HMAC 校验、签名 URL 时效、状态机白名单、金额换算——每处通常只有几十行，全读；
- **高风险路径的测试断言**：测试是其他一切验证的支点，测试质量没有人替你审。看三点——断言强度（断言了具体金额，还是只断言"不报错"）、负例是否存在、mock 边界是否恰好把要验证的东西 mock 掉了；
- **契约定义**：schema、状态表、权限矩阵——小而中心；
- **每个 PR 的 diff 文件形状**：不求逐行理解，扫一眼改了哪些文件，即可抓住越界修改。

**可以委托的（填充物）**：UI 装配、CRUD 胶水、列表筛选、样式——被类型系统与 E2E 夹住，写歪会被机器拒绝。

**委托的硬边界（相关盲区判据）**：真正独立的验证通道是确定性的——类型检查、测试执行、RLS 数据库层拒绝、HMAC 数学验证；Agent 会话之间不算相互独立（§3.2）。因此，某类风险若只有"Agent 写 + Agent 审"两道防线、没有确定性闸门兜底，该类代码必须亲读；有确定性闸门兜底的，才允许委托。

**检验标准**：读码量不是先验设定，由逃逸缺陷归因校准（§8.1）——归因反复落在"测试/契约缺口"，补闸门即可；反复落在"只有人读实现才能发现"，即读码量过低的确凿信号，对应模块升一档审查；长期零逃逸且零检出，先怀疑检测在漏，而不是庆祝。

**下限维持程序**：本节全部判据都建立在审判能力下限之上（L4 协议 §3.1 的协议失效条件）。该下限的建立、训练与验收程序见 [[3.Ai_native_coding_engineering|Ai_native_coding_engineering]]——四文档体系的人侧操作层。逃逸缺陷执行**双登记**：闸门归因（哪道闸门本应拦住，§8.1 第 5 条）进仓库 R 账本（主控台 §6 风险索引）；能力归因（没读到 / 读到没懂 / 懂了没当回事）进学习日志（[[学习日志]]，该文 §十一.5 漏检归账）。

## 6. 机器闸门、命令速查与环境陷阱

### 6.1 CI 门禁

**Basic Gates**（`.github/workflows/basic-gates.yml`，PR 与 push main 均触发）四个 job：

| Job | 内容 |
|---|---|
| repository-baseline | 空白/冲突标记检查；拦截本地专用文件入库；Gitleaks 密钥扫描 |
| docs-lint | 治理脚本自测 + `docs_lint.py` 文档义务机器闸门（编号连续性、引用完整性、账本一致性） |
| app-ci | `pnpm lint` → `typecheck` → `test:coverage`（覆盖率阈值防回退）→ `build` → `secret:scan` |
| e2e-smoke | Playwright chromium 全量 E2E |

**DB Migration Test**（`.github/workflows/db-migration-test.yml`）：`apps/fabbrio/server/**` 等路径变更时触发；用 Neon branch 建隔离库跑迁移漂移校验、`db:migrate`、`test:db` 契约测试，结束销毁分支。

**pre-commit hooks**（`.githooks/`，需 `git config core.hooksPath .githooks` 启用）：暂存区空白/冲突检查、本地文件拦截、gitleaks 密钥扫描；`commit-msg` 强制 `type(scope): 摘要` 格式。

### 6.2 命令速查

| 目的 | 命令 |
|---|---|
| 单测 / 覆盖率 | `pnpm test` / `pnpm --filter @fabbrio/web test:coverage` |
| 类型 / 风格 | `pnpm typecheck` / `pnpm lint` |
| 构建 | `pnpm build` |
| E2E | `pnpm --filter @fabbrio/web test:e2e` |
| DB 契约测试 | `pnpm --filter @fabbrio/web test:db`（需 `RUN_DB_TESTS=1` 与数据库） |
| 密钥扫描 | `pnpm secret:scan` |
| 文档闸门 | `python scripts/docs_lint.py` |
| 主控台派生区同步 | `python scripts/assemble_status.py`（改 §4.1 后必跑） |
| 领取新编号 | `python scripts/next_id.py <VR\|DEC\|R>` |

### 6.3 已知环境陷阱（Windows 实战验证）

这些是本项目真实踩过、已确认解法的坑，新会话遇到同症状先对照此表，不要重新诊断：

- **pnpm script-shell**：Windows 上必须把 pnpm 的 script-shell 设为 Git Bash，否则 `typecheck`/`dev`/`test` 全部失败；
- **autocrlf 幽灵修改**：`autocrlf=true` 且无 `.gitattributes` 时会出现零内容差异的 M 状态阻断 rebase——核证 `git diff` 为空后 `git checkout -- <file>` 归一即可；
- **幽灵删除根级中文文件**：`docs_lint` 突然爆 100+ 引用错误时，先看 `git status` 是否有根级 SOP/范式文件的 D 行，`git checkout HEAD -- <file>` 还原即可，真因不在文档内容；
- **gitleaks 误拦**：`token`/`secret` 命名后跟长标识符赋值会被 pre-commit 拦截，用间接键名或缩短右值绕开；
- **覆盖率波动**：vitest 全局 All-files 覆盖率跨运行有 ±3 单位波动，复核比对看逐模块数字，不看全局。

## 7. 证据分级与高风险放行清单

### 7.1 证据分级

证据必须分级表达，不同级别只能证明不同范围的事实：

1. **单元、契约、E2E 与 CI 通过**：证明相应实现在受控环境下的行为；
2. **测试店或 mock**：只证明测试环境中的行为，**不能外推为真实链路**；
3. **负责人使用真实凭据完成真实验证**：才可证明真实链路；
4. **未验证项**：明确写"待负责人亲执"或"上线前复验"，绝不外推。

### 7.2 高风险放行清单

遇到报价、资金、状态机、权限、文件、AI、数据库或第三方写入，逐项确认：

- [ ] 事实源、写权限和失败恢复已明确；
- [ ] 正常、边界、拒绝、重复请求和第三方失败均有测试；
- [ ] 前端没有承担算价或特权写入；
- [ ] 跨公司读取/下载与 404 掩蔽没有绕过授权；
- [ ] 金额、币种、Quote 与 Draft Order 的对应关系已核对；
- [ ] Webhook 签名、幂等与重试/人工恢复策略明确；
- [ ] 独立会话已复核关键 diff；
- [ ] 负责人已完成必须的人工闸门；
- [ ] `main` 侧 CI 已回读，剩余风险已登记编号。

**任一项不成立，就不是"已完成"，最多是"实现已提交、待验证"。**

## 8. 节律：合并后、每周、熔断

### 8.1 每次合并后

1. 回读 Git、PR 与 `main` 侧 Basic Gates；
2. 确认主控台没有把"待验证"写成"已完成"；
3. 将验证证据（VR）与剩余风险（R）写入正确位置；
4. 判断是否出现新的真实集成、权限、金额或运营风险；
5. 若发现**逃逸缺陷**（越过闸门抵达 main 或更远的问题），登记 R 编号时强制归因一问——**哪道闸门本应拦住它**。归因分布是校准读码量与补闸门优先级的依据（§5.3 检验标准）。

### 8.2 每周

1. 复核待办是否仍有明确负责人和放行条件；
2. 只抽查最高风险或变化最大的 1–2 条业务链路；
3. 将重复出现、已验证有效的做法沉淀为模板或 Skill（遵守 L4 协议 §3.3 资产化阈值——不是每个经验都值得固化）；
4. 不把没有证据的"看起来没问题"沉淀成规则。

### 8.3 熔断（联动 L4 协议 §6.3）

出现以下任一信号，立即停止 AI 交互，回归身体与真实环境，恢复后先复盘再开工：

- 机械堆砌文档、提示词或复核轮次，却说不清当前业务问题；
- 身体僵硬、呼吸变浅、注意力碎裂、对工具产生强迫性依赖；
- 产出速度上升，但判断质量和真实感下降。

本项目的高发场景是**长 rebase、编号撞号与多轮返工循环**——连续机械返工超过两轮仍未收敛时，先停下来问"是流程缺口还是我在硬扛"，再决定继续。从未触发过熔断，更可能说明信号被忽略，而不是状态完美。

## 9. 作为 L4 第一实践：验证目标与诚实清单

### 9.1 三次编译在 Fabbrio 的映射

L4 协议的核心是三次连续编译，本项目是它的第一块试验田：

| 编译 | Fabbrio 载体 |
|---|---|
| 模糊意图 → 共享模型 | 需求文档、业务信息流图、任务书（项目管理会话产出） |
| 共享模型 → 可执行约束 | `AGENTS.md` 产品契约、ADR、Drizzle schema、状态机定义、测试与覆盖率阈值 |
| 执行结果 → 可信证据与可继承资产 | Basic Gates、VR/DEC/R 账本、SOP 与范式文档、失败案例教训 |

### 9.2 本项目要验证的 L4 命题

1. **图能否转化为契约**——每个新功能单元有信息流图，且能指出它编译到了哪个 schema、哪组测试；
2. **契约能否转化为门禁**——高风险规则由 CI 与测试自动拒绝，而不是靠人的记忆盯守；
3. **门禁能否持续产生可信交付**——合并后 `main` CI 持续绿、证据链随机抽查可走通；
4. **认知负荷是否可持续**——熔断机制被真实使用，返工率随资产沉淀下降，而不是靠透支硬扛。

这四条任何一条长期不成立，说明 L4 协议需要修订，而不是加倍努力执行——这正是协议 §4.5 元 Harness 的用途。四命题的**首次强制复核挂在 PM-705 上线复盘**（见 §10.3）：复核结论登记进认知变更日志与主控台，不依赖"有空再看"。

### 9.3 诚实清单：已验证 vs 待验证（※2026-07-21）

**已在 80+ 个任务中反复验证的做法**：契约先行、TDD、多会话独立复核、证据链交付、VR/DEC/R 账本、Basic Gates 四 job、编号顺延与撞号处置、文档治理审计。

**尚未验证、不得外推的敞口**（每项都有编号与触发条件，不靠记忆）：

- **真实集成批未执行**（PM-609/610/611）：目前所有 Shopify、对象存储、LLM 行为只在测试店与 mock 层被证明，真实凭据链路待负责人亲执；
- **上线前门禁池挂起**：MAN-T3、商户审核（MAN-MER 八项）、Webhook 真实验证与真实发布等人工闸门待上线前统一执行（Go/No-Go 判据见第 10 章）；
- **发布与运维层已规划、内容待填充**：P7 批次（PM-701–706）覆盖发布方案、上线 SOP 与回滚、env/secret/权限交接、生产部署执行、上线记录与复盘；`文档/01-开发文档/6.发布与运维/` 三份文件目前为占位。数据备份/恢复演练尚未显式写入任何任务的完成条件，已提请 PM 补登；
- **平台级分支保护待配置**：GitHub Pro 已就绪，配置属遗留行动项，当前靠 DEC-001 流程约定（负责人亲手合并）兜底；
- **文档存在个别漂移**：如 `AGENTS.md` 中 E2E 规模数字落后于实际（写 11 spec/73 例，实际 13 spec/96 例），说明 T1 例行对齐需要保持节奏。

**结论**：在这套体系下，"编程能力弱"本身不再是**未被管理的**风险敞口——已枚举的敞口全部在上面的待验证清单里，每一项都有编号、负责人和触发条件。仍然由人承担的敞口是**人工闸门的裁决质量**：接受什么证据、放行什么风险。这部分不可外包，靠承重结构亲读（§5.3）与审判能力下限（L4 协议 §3.1）维持。这正是 L4 所说的"让正确执行不依赖于人的即时在场"——即时在场可以省，裁决在场不能省。

## 10. 上线就绪与 Go/No-Go

前面九章控制"到达 main 的东西是否可信"，本章回答"什么时候可以上线"。前提必须诚实：**本手册无法确保上线——上线由执行剩余任务链保证。**手册能确保的是三件事：没有未知的阻塞项（完备性）、每个已知阻塞项有编号/负责人/触发条件（可追踪）、上线决策基于证据而非感觉（可裁决）。

### 10.1 上线关键路径（※2026-07-21）

```
PM-612 收尾（P6 清尾）
  → PM-609 / 610 / 611 真实集成批
    （证据从第 2 级升到第 3 级；含 MAN-MER 八项建号，负责人亲执）
  → PM-701 发布运维方案 → PM-702 上线 SOP + 回滚说明 → PM-703 env/secret/权限交接
  → 【Go/No-Go 闸门（10.2）】
  → PM-706 生产环境搭建与部署执行（真发，负责人亲执）
  → PM-704 上线记录回写 → PM-705 复盘与长期风险回写（含 L4 四命题首次复核）
```

**真实集成批是决定性瓶颈**：这一批不完成，Go/No-Go 清单永远不可能全绿。它应排在 P7 文档任务之前或与之并行，而不是排在最后。

### 10.2 Go/No-Go 判据（PM-706 执行前的强制人工闸门）

只有一条规则：**清单全绿才 Go；任何一项不绿就 No-Go，没有"基本上可以"。**

- [ ] 真实集成批（PM-609/610/611）VR 记录齐全，证据全部为第 3 级（真实凭据验证）；
- [ ] 上线前门禁池逐项有负责人签核：MAN-T3、MAN-MER 八项、Webhook 真实验证、`CRON_SECRET` 生产配置等；
- [ ] 回滚说明（PM-702）存在且**被负责人亲手演练过一次**——未演练的回滚方案等于没有；
- [ ] 数据备份与恢复至少演练一次——Neon 有 PITR/分支能力 ≠ 恢复流程被验证过；
- [ ] 高风险放行清单（§7.2）对最后一次合并后的主干完整走一遍；
- [ ] 上线后 48 小时观察方案明确：看哪些指标、什么信号触发回滚、谁值守。

本清单应挂 DEC 编号登记进主控台，成为账本的一部分而不是散文。

### 10.3 上线后节律

- **前 48 小时**：按观察方案值守；出现回滚触发信号时执行回滚，不现场辩论；
- **第一周**：每日过一遍 webhook 对账结果、错误日志，抽查订单/报价链路各一条；
- **复盘（PM-705）**：登记上线记录与长期风险，并完成 L4 四命题的首次强制复核（§9.2），结论进认知变更日志。

**运维事实源**：部署、上线、回滚的具体步骤以仓库 `文档/01-开发文档/6.发布与运维/`（01-部署说明 / 02-上线记录 / 03-回滚说明）为准。本章只保存判据与入口，不复制步骤，避免两处维护。

## 附录：给 Agent 的任务说明模板（变更契约）

```
目标：
<用户要完成的业务动作；明确不做什么>

事实源与裁决顺序：
<AGENTS.md → 规格卡/ADR → 主控台 → 当前代码；冲突先登记后裁决>

允许修改范围：
<目录、模块、接口>

禁止修改范围：
<报价引擎、状态机、鉴权、既有接口等；触碰即停>

关键规则与风险：
<权限、公司隔离、金额真值、Tier 边界、异常路径>

验收：
<AC ↔ TC 对应；需实际运行的命令；是否需要 E2E / DB 测试 / 人工验证>

完成交付：
改动清单 / 信息流说明 / 测试实际输出 / 文档回写（功能清单、主控台、VR）/ 剩余风险
```

**使用要求**：让 Agent 先复述任务理解和风险，再开始实现——这样你在不精通代码时，仍能有效判断它是否理解了任务。

---

## 最终判断标准

你成功的标志不是能否亲自写完 TypeScript，而是在 Agent 高速产码时持续做到：

- 看懂业务行为与信息流；
- 抓住事实源、写权限、状态、金额与公司隔离；
- 通过关键代码抽查和独立复核识别错误；
- 用测试、CI、人工闸门和回读证据做完成判断；
- 对未验证风险保持诚实、可追踪、可恢复。

这就是 AI Native 技术负责人的核心能力：**不替代 Agent 编码，但控制系统是否值得被交付。**

---

**参考**：
- [[0.2.AI 时代 L4 基础设施构建者：全链路认知与执行协议|AI 时代 L4 基础设施构建者：全链路认知与执行协议]]（同目录）
- [[0.1.AI时代原生人类：认知、学习与价值创造方式的重构|AI时代原生人类：认知、学习与价值创造方式的重构]]（同目录）
- [AI Native 技术负责人执行手册（Notion 原版）](https://app.notion.com/p/3a469d4cb95281c5be3bc9775aed230d)
- [AI时代原生人类（Notion 原版）](https://app.notion.com/p/3a069d4cb952811d8303eda13d3d9e60)

**版本**：v1.3（v1.0 基于 Notion 执行手册 + L4 协议 v2.0 + 仓库实况编写；v1.1 新增第 10 章上线就绪与 Go/No-Go、修复双链、敞口清单对齐 P7 实况、四命题复核挂点 PM-705；v1.2 新增 §5.3 承重结构最小读码集与相关盲区判据、§3.2 模型独立性注记、§8.1 逃逸缺陷归因、§9.3 敞口结论措辞收敛；v1.3 §5.3 补下限维持程序——人侧操作层 Ai_native_coding_engineering 编入四文档体系、逃逸缺陷双登记约定、§0 三层表扩为四文档表、两本账位置标注〔R 账本=主控台 §6·学习日志=2.Agent_harness_engineering，均不另建副本〕）
**更新日期**：2026-07-22
