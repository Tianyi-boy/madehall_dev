# Madehall 统一原型问题登记表 · Codex v1.0

> 格式沿用《18》五列；“claude”列名按原表契约保留。合并前已有问题只登记不改；矩阵口径缺口只登记。

| 元素 | 问题 | 建议 | claude实查 | claude修改建议 |
|---|---|---|---|---|
| M15-01 · A 页历史状态 | iframe `srcdoc` 中 `history.replaceState` 越权，A 页首次切换报错 | 仅在合并壳适配内部页状态 | ❌ 合并引入；已修并复验 A 跨视角回跳 | 用 `window.__A_HASH`，不触碰原业务 DOM/文案 |
| M15-02 · 动态按钮 | 运行时模板会重新产生真实 `onclick` 属性 | 统一转入委托属性 | ❌ 合并引入；已修，三视角浏览器读回 `onclick=0` | MutationObserver 只做机械属性迁移 |
| M15-03 · 跨视角路由 | B iframe 初次加载时 hash 初始化晚于父路由调用 | 区分首载与已载 | ❌ 合并引入；已修，配置/套餐/回店面均落正确页 | 父壳 `MADEHALL.route` 单点适配 |
| M15-04 · favicon | 浏览器默认请求 `/favicon.ico` 产生 404 | 内联空 favicon | ❌ 合并引入；已修，控制台 0 | 父/子均用 data URI，不引新文件 |
| F14-02 · MT-320 容器参考价 | 无确认价类型在容器行如何展示未定义 | 维持 B 稿回避，等负责人裁 | 🔍 矩阵口径缺口；本批零触碰 | 登 B-30，建议“参考价未确认且不计合计” |
| F14-06 · 人工通道计数 | 矩阵仍含已撤销代录立项，8/9/10 处计数互相冲突 | 下次矩阵升版订正 | 🔍 矩阵口径缺口；只登记 | 不在原型内造新口径 |
| F14-07 · C-12 前提 | 行级三档引文未随 v4.9 收窄为 ∩{T1,T2} | 补前提收窄注 | 🔍 矩阵口径缺口；只登记 | 保留登记时左栏引文不动 |

结论：本次未发现“合并前已存在且不属于停用/已知项”的新增实现缺陷；4 个合并适配缺陷均已并入合并日志并复验。
